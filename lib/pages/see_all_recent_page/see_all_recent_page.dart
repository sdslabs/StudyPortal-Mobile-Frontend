import 'package:flutter/material.dart';

class SeeAllRecentPage extends StatelessWidget {
  const SeeAllRecentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("See All Recent Page"),
      ),
    );
  }
}
