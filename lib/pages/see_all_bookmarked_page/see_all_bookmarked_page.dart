import 'package:flutter/material.dart';

class SeeAllBookmarkedPage extends StatelessWidget {
  const SeeAllBookmarkedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("See All Bookmarked Page"),
      ),
    );
  }
}
